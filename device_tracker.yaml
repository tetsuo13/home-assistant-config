# https://home-assistant.io/components/device_tracker/

# Using SNMP doesn't track when the device leaves.
#  - platform: snmp
#    host: 10.8.13.1
#    community: public
#    baseoid: 1.3.6.1.2.1.4.22.1.2
#    new_device_defaults:
#      track_new_devices: False

 - platform: unifi
   host: 127.0.0.1
   username: !secret ubiquiti_username
   password: !secret ubiquiti_password
   verify_ssl: false
   detection_time: 60
   ssid_filter:
     - !secret ubiquiti_ssid

